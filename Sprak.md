```
    # this is a comment

    void Foo(string x)
        return x + "!"
    end

    array stuff = [1, 2, "three", false]

    loop x from 1 to 10
        loop y in stuff
    end

    if Foo("hej") == "hej!"
        number x = 10
        number y = 20
        Print(x * y)
    end
```